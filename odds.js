export default async function handler(req, res) { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET,OPTIONS'); if (req.method === 'OPTIONS') { res.status(200).end(); return; } const API_KEY = '4eb58d5bb37abad36be528916b173e55'; const { sport, regions = 'us,uk', markets = 'h2h', oddsFormat = 'decimal' } = req.query; if (!sport) { res.status(400).json({ error: 'sport param required' }); return; } try { const url = `https://api.the-odds-api.com/v4/sports/${sport}/odds?apiKey=${API_KEY}&regions=${regions}&markets=${markets}&oddsFormat=${oddsFormat}`; const response = await fetch(url); const data = await response.json(); const remaining = response.headers.get('x-requests-remaining'); const used = response.headers.get('x-requests-used'); res.status(200).json({ data, remaining, used }); } catch (err) { res.status(500).json({ error: err.message }); } }